# 2026-08-15：翎风 TXT 兼容灰度候选

## 新增

- 原生 TXT 物理来源、双目录布局、组合来源和冲突诊断；
- 快照热重载、失败回滚、摘要和手动重载；
- 基础语法、控制流、统一变量和已审计高频命令包；
- 登录与升级系统触发适配；
- 战斗伤害前后、成功拾取、怪物死亡和掉落完成触发的受控兼容子集；
- HTTPS 精确主机白名单与独立高风险 Kill Switch；
- 可复核的灰度候选脚本、配置片段、SHA-256 清单和回滚片段。

## 客户端

- `NPCResponse` 沿用共享协议，中文、换行和内联按钮往返测试通过；
- PC 与 MonoGame Android 均完成构建；
- Android x64 模拟设备真实启动、自动进服验证通过；
- Android 模拟器已实际打开灰度 NPC `[@MAIN]` 并点击进入 `[@VERIFY]`；中文、换行、内联按钮与服务端运行时分派记录一致；
- 修复 Android 启动期同步 GPU 贴图预热导致的 ANR，改为后台字节预取与按需纹理创建。

## 兼容边界

本候选只承诺兼容清单中的 B/C 条目。D 为未实现，E 为依赖不足，X 为明确排除；未知 `?` 为零。高风险命令默认关闭，`OPENBROWSER` 仅提供受控 HTTPS 安全替代。

## 升级与回滚

先以 `CSharpFirst` 加载候选并检查冲突，再按受控 Key 切换来源。出现未知命令、重复奖励、持久变量异常、主线程阻塞、协议错误或热更失败时，立即应用候选目录中的回滚片段并恢复上一脚本快照。

候选已在项目所有者指定的真实服务器完成部署前完整备份、360 秒保存周期、优雅退出、冷启动和数据库持久化核对；脚本错误与未知命令均为 0。Android 模拟器 NPC 实交互也已完成。该结果标记本规格要求的受控灰度验证完成，不替代更长期的生产流量观察。
