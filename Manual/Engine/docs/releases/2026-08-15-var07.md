# 2026-08-15 变量系统 VAR-07

## 新增

- `SETCURRTARGET` 与只读 `C.STR/C.HUMAN/C.GUILD` 当前目标访问。
- 在线角色之间的 `SETHUMVAR` 与 `GETHUMVAR` 显式变量传递。
- `LegacyCurrent`、`Audit`、`LingFengCompatible` 三种启动模式。
- UTF-8/CP936 脚本只读预检、固定前缀统计、逐行诊断与内容摘要。
- `--variable-preflight <根目录>` 命令行预检入口。
- TXT/INI/CS 一体扫描与只读占位符清单。
- `LOADVALUE/SAVEVALUE/%A*` 与统一 A 全服持久存储对齐。

## 安全与兼容

- 当前目标必须在线、同图且不超过 20 格；每次读取重新验证。
- `C.` 只读；写目标必须经过统一变量模块。
- 跨角色操作第一版不支持离线角色。
- 兼容模式绑定实际脚本摘要；脚本变化后必须重新审核。
- 原始 NPC A 临时分流没有恢复，A 固定及命名变量继续使用统一全服持久语义。

## 验证

- 变量模块、预检与真实内容定向测试 63 项通过。
- `Server.MirForms` Release 构建 0 错误。
- 正式宿主进程输出 `LEGACY_A_ADAPTER=True`、`CROSS_OBJECT=True` 与 `COMPATIBILITY_PREFLIGHT=True`。

## 真实项目验收

指定 `Server-mono/Envir` 快照共 2348 个文件，摘要为 `7E1E13532F37151BBC15E4B7B43383A8C1D95E3B0DC25944A2F19864D74ABE3D`，预检 0 个阻断错误。A 的 36 处引用全部定位到“定位符咒”的两份等价生成内容并完成语义审核；47 种只读系统占位符均由现有解析器支持。脚本快照变化后仍必须按[兼容模式与迁移](../scripting/variables/compatibility-migration.md)重新审核，不能沿用本摘要。
