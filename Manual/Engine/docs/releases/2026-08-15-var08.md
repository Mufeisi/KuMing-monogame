# 2026-08-15 变量系统 VAR-08

## 交付

- 预检现可统计 TXT、INI 和转换后 C# 内容中的只读系统占位符。
- 指定真实项目引用的 47 种占位符已逐项与现有 `NPCSegment` 解析器核对，无需重复实现适配器。
- 新增已审核占位符门禁；新符号必须先完成需求、权限和隐私审查。
- 密码、密保、电话和邮箱类占位符明确失败关闭。
- TXT `VAR` 声明与 C# 声明进入同一候选注册表并原子热重载。
- `INITVAR` 条件初始化与 `PARSEDECIMAL` 字符串显式转小数完成进程级验证。

## 兼容

本阶段没有改变 47 种已有占位符的显示含义，只为兼容模式增加清单与安全门禁。`STR`、`FORMAT` 和 `C` 是变量系统自身的受控显示入口。

## 验证

- 真实快照：2348 个文件、47 种占位符、0 个阻断错误。
- 真实项目 C# 脚本编译与世界语义预检通过。
- 未审核符号返回 `VAR08-UNKNOWN-001`；敏感符号返回 `VAR08-SENSITIVE-001`。
- Base05 全量 591 项、变量及真实内容专项 64 项、持久 Provider 专项 10 项通过。
- 独立服务端进程完成脚本热重载、NPC 命令、角色小退、服务停启和持久恢复，退出码为 0。
- 网页说明书严格构建及必要搜索词门禁通过，离线 ZIP 具有提交、工具链和 SHA-256 清单。

完整清单见[只读系统占位符](../scripting/variables/read-only-symbols.md)。
