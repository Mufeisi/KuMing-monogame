# AGENTS.md —— 本仓库 Agent 执行铁律（Codex / Claude 必读）

> 本文件是 Codex 在本仓库执行任务的**最高执行约束**。开工前必须通读；执行中反复对照。

## 总纲（一句话）

**交付"能跑的东西"，不交付"关于怎么干活的东西"。**
判断标准：**用户能不能直接看到/用到今天的产出？** 能→继续；不能→停下来重新想，不许接着造。

## 铁律

1. **禁止造机器**：任何"我先造个 X 才能继续"的冲动，默认禁止。找文件用 `grep`/`rg`；算差集用一次性脚本跑完即弃；**不造**分析器/矩阵/迁移工具/审核系统。造工具必须先在简报里说明并经确认。
2. **当前唯一目标**：只做 PRD 对应 GATE 的退出条件。发现别的 bug → 记 backlog 不改；发现更好架构 → 记候选不动；下一阶段的事 → 一律不做。
3. **完成定义 = 工件**：每个子任务必须产出 代码/diff、运行输出、截图、或对比数据。**写报告/画图/列清单 ≠ 完成。**
4. **任务简报模板**：开工先写"目标/做/不做/方法约束/预估时间/完成定义"。写不出完成定义 = 任务没拆清，先回去拆。
5. **时间预算与打转报警**：连续 3 轮无代码改动、超过预估 2×、开始造工具、同一问题 2 次无进展 → **立即停下报告，禁止换更大的工具再来一次**。停止后给 2~3 个选项交用户拍板。
6. **每日工件检查**：过程资产（分析/计划/矩阵/审核）数量 ≥ 工件数量 → 明天必须换策略。
7. **禁止重造**：渲染接缝（DXManager/SpriteBatchStack）、脚本热更（Server/Scripting）、微端（Bootstrap*）、FairyGUI 运行时、协议序列化（Shared/Packet.cs）、SchemaMigrator 全部已有且可用，**禁止重写**。
8. **边界敏感**：协议改 Shared 不碰 Share 副本；玩家状态只主线程写；脚本只旁路 Hook；渲染走接缝；DB 改表走 SchemaMigration；发布物禁止 Preview。
9. **升级层级**：产品/门禁/定义级问题（验收标准、阶段范围）→ 升级给用户；技术实现选择（A/B 方案、摘要模型）→ 自己定并记录，**禁止把 1/2/3 类技术选项抛给用户**。
10. **定义升级出口**：验收标准在当前基建下结构性不可满足（如 11GB 外部资源仅存本机、无 CI → "裸 clone 可重建"无法验证）→ **禁止硬试**，先升级定义（拆成可完成项 + 独立后续项）。
11. **验证成本分层**：单次验证 > 30 分钟 → 小改动只跑快内环（局部校验），慢外环（全量 acquire/完整发布）只在阶段末或明确的重试验证时跑，每次重跑前先书面说明"为什么这次值得跑"。
12. **语言纪律（中文保持）**：**本仓库全部交流/文档/状态报告/commit message 一律中文**，英文仅限代码标识符、命令、报错原文、不可翻译的技术名词。**整段英文输出 = 违规**：触发停止条件，立即停下，用中文重发。任务简报、每日工件检查必须含"语言"项。当上下文含大量英文工具输出时，主动用中文转述关键信息，减少英文对上下文的污染（防"语言跟随效应"导致输出漂移）。

## 完整版

详见 `Docs/执行纪律与防走偏铁律.md`（含历史违规案例、停止条件、禁止重造清单、任务简报模板）。PRD 阶段门禁见 `Docs/PRD-PC稳定与Android发布.md`（GATE-P0..P5）；架构决策见 `Docs/继续开发架构设计报告.md`（ADR）。

## 标准软件工程接入

- 功能开发、缺陷修复、重构、性能、安全、数据库、工程治理、文档整理、审查和发布任务，必须使用个人技能 `standard-software-engineering`；技能入口为 `C:\Users\luo\.codex\skills\standard-software-engineering\SKILL.md`。
- 项目落地规则、文档职责、任务生命周期和质量门禁见 `Docs/工程开发规范.md`；文档权威来源与阅读入口见 `Docs/index.md`。
- 技能是通用执行基线，不取代本文件、PRD 或已接受 ADR；冲突时仍按下方“冲突仲裁”处理。
- 能自动验证的 MUST 规则应逐步进入 CI、测试、分支保护或发布流水线；在自动化前，执行者必须提供对应验证证据，不得仅以文字声明完成。

## 冲突仲裁

- 本文件 vs 任务简报 → 以本文件为准。
- 本文件 vs PRD → 以 PRD 为准并报告冲突。
