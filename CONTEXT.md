# 启动器选区上下文

本上下文描述玩家在启动器中获取区服目录、选择区服并进入游戏时使用的统一语言。

## 语言

**GM 启动器项目**：
GM 在编辑器中独立维护的一套玩家入口、品牌主题、区服目录、发布源、签名信任和客户端发行设置。
_避免使用_：编辑器配置、皮肤文件、服务器配置

**项目恢复包**：
用于在设备损坏或迁移后恢复一个 GM 启动器项目的加密离线工件，包含项目定义、主题源素材和受密码保护的签名密钥，但不包含客户端发行体。
_避免使用_：项目导出、客户端备份、明文密钥包

**离线部署包**：
由编辑器生成、已完成签名与校验且不包含私钥的发布工件，可复制到联网服务器后直接导入并发布。
_避免使用_：项目恢复包、玩家入口、源码包

**远程启动清单**：
由启动器获取的版本化 JSON 文档，包含全局补丁通道配置和区服目录；它是可静态托管的数据，不是游戏连接地址。
_避免使用_：静态网页、远程区服列表、游戏地址

**清单来源**：
启动器读取远程启动清单时使用的 HTTP 或 HTTPS URL。
_避免使用_：游戏入口、状态端口

**区服条目**：
玩家可在启动器中选择的一个游戏区服，包含显示名称、游戏入口和该区服的微端配置。
_避免使用_：线路、节点

**区服分组**：
GM 用于组织区服目录的单层显示分类；区服可以归入一个分组，分组不改变区服的连接、补丁或微端行为。
_避免使用_：父区服、线路组、多级目录

**区服状态**：
GM 随区服目录发布的运营状态，用于显示推荐、新区、维护等标识；第一版不表示实时在线人数、网络延迟或服务端探测结果。
_避免使用_：实时状态、在线状态、延迟状态

**已选区服**：
玩家在启动器中选定、将在点击“开始游戏”后生效的区服条目；仅改变选择不会立即启动游戏。
_避免使用_：默认区服、已连接区服

**区服选择模式**：
主题向玩家呈现同一份区服目录时采用的交互形式；由 GM 在发布主题时固定，当前支持侧边栏展开式和下拉选择式，不改变区服条目的含义与连接行为。
_避免使用_：列表协议、区服列表类型

**启动器会话**：
从启动器显示到玩家主动关闭启动器的完整使用周期；期间游戏可以启动和退出多次，游戏退出不结束该会话。
_避免使用_：单次游戏会话、游戏进程

**游戏实例**：
由启动器为一次“开始游戏”操作创建的独立客户端运行实例；同一启动器会话可以同时管理多个游戏实例。
_避免使用_：游戏窗口、区服条目

**多开上限**：
一个启动器会话允许同时存活的游戏实例数量上限。
_避免使用_：账号上限、服务器连接上限

**游戏入口**：
游戏客户端连接某个区服所需的主机与端口。
_避免使用_：列表地址、网页地址

**微端入口**：
某个区服按需获取客户端资源时使用的服务地址；是否使用该入口由同一区服条目明确声明。
_避免使用_：游戏入口、补丁地址

**默认微端入口**：
一个 GM 启动器项目内全部区服默认共享的微端入口；特殊区服可以显式覆盖，但覆盖资源必须与同一客户端发行体兼容。
_避免使用_：默认区服、游戏入口、全局补丁通道

**备用微端入口**：
与主微端入口提供相同资源版本和签名身份的可选故障转移入口；客户端仅在主入口持续不可用时按会话切换。
_避免使用_：负载均衡节点、区服线路、不同资源库

**微端网关**：
面向玩家提供启动核心、资源分包和按需资源访问的独立运行服务，负责连接、基础配置、文件读取与缓存，不承担游戏逻辑。
_避免使用_：游戏服务器、启动器发布源、微端入口

**微端资源库**：
GM 上传到微端服务器并由微端网关扫描的一套完整客户端资源目录，是启动核心、资源分包和按需资源响应的本地事实源。
_避免使用_：玩家缓存、微端切片缓存、启动器发布目录

**客户端发行体**：
由一个启动器管理的一套客户端程序与资源；同一发行体中的全部区服共享相同的客户端版本。
_避免使用_：单区客户端、区服补丁

**客户端交付模式**：
GM 为客户端发行体选择的初始交付方式；默认微端交付只先提供可运行核心并按需补齐资源，完整交付则预先提供全部客户端资源。
_避免使用_：启动器模式、区服模式、补丁模式

**启动核心**：
玩家在没有现成客户端时进入登录与基础游戏流程所必需的最小程序和资源集合；它不代表完整客户端发行体。
_避免使用_：完整客户端、启动器皮肤、微端缓存

**玩家入口**：
玩家首次获取并主动运行的唯一文件；启动所需的其余配置与资源应由该入口自动发现、获取或恢复。
_避免使用_：安装包、客户端压缩包、启动器目录

**客户端位置**：
玩家入口用于读取、更新并启动客户端发行体的本地根目录；应优先自动发现，仅在无法确定时要求玩家选择。
_避免使用_：启动器目录、游戏入口

**内置主题**：
随玩家入口提供、离线时也能完整显示和操作的启动器主题，是远程主题不可用时的最终保底。
_避免使用_：写死皮肤、临时皮肤

**有效主题快照**：
最近一次完整下载且通过校验的远程主题；获取新主题失败时继续使用该快照，不得留下图片与样式混用的半成品界面。
_避免使用_：在线皮肤、图片缓存

**启动器发布源**：
玩家入口获取区服目录与主题版本的公开只读位置；可由游戏服务器托管，也可使用外部静态网站或 CDN。
_避免使用_：微端入口、管理接口、上传接口

**已签名启动器版本**：
由 GM 编辑器发布并通过可信公钥验证的完整区服与主题版本；其清单同时约束版本序列和全部文件摘要，验证失败时不得启用其中任何内容。
_避免使用_：远程文件、在线配置、部分更新

**全局补丁通道**：
为整个客户端发行体发布程序与资源更新的唯一补丁来源，不隶属于任何单个区服条目。
_避免使用_：区服补丁、微端入口

**有效列表快照**：
最近一次成功获取且整体通过校验的远程启动清单；下载失败或内容无效时不得覆盖它。
_避免使用_：临时响应、半成品缓存

**保底区服**：
设备上不存在有效列表快照时，由本地配置提供的单一区服选择。
_避免使用_：默认缓存、远程区服
