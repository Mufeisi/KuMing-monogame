# 启动器保留并协调独立游戏实例

启动器作为常驻父进程运行，每次点击“开始游戏”都创建一个独立游戏子进程，并将当次已选区服应用于该游戏实例。这样多个游戏实例不会共享现有客户端的渲染、声音、网络等全局静态状态；游戏退出不结束启动器会话，只有玩家主动关闭启动器才结束启动器。启动器负责跟踪自己创建且仍存活的游戏实例，并按多开上限决定能否继续启动。游戏运行期间启动器只最小化到 Windows 任务栏，不创建托盘图标或托盘菜单；最后一个游戏实例退出后自动恢复启动器。
