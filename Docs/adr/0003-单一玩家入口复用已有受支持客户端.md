# 单一玩家入口复用已有受支持客户端

玩家只获取并运行一个入口文件。入口只复用带有效 `launcher-capabilities.json` 且声明 Current15Arguments 的当前受支持客户端；无法确认兼容或不存在可用客户端时，自动建立客户端位置并释放内嵌启动核心。Legacy11 与 DLL 启发式探测已因微端凭据和普通资源阻断边界无法保证而移除。未标记旧客户端只读拒绝，不在原目录覆盖或写入能力标记；未来若支持旧客户端迁移，必须单独设计可恢复事务并使用真实旧二进制验证。
