# 微端入口项目级复用并允许区服覆盖

> 实施状态：部分实现。项目默认入口、User 同步和区服覆盖已实现；覆盖网关的资源版本兼容校验尚未实现，列入实施规格第 10 节。

GM 启动器项目统一配置默认微端入口、访问用户和 Code，全部共享同一客户端发行体的区服自动复用；只有特殊区服才显式覆盖微端入口。编辑器必须验证覆盖网关的资源版本兼容，避免 GM 为每个区服重复配置，也避免不同客户端资源被错误混用。
