# 启动器编辑器示例项目

把整个 `launcher-editor-sample` 目录复制到编辑器工作区，然后启动编辑器即可打开。

- 首次加载会为当前副本生成独立发布密钥和随机微端 User；私钥只写入本机受保护的 `.secrets`，不会包含在本示例中。
- `192.0.2.10`、`198.51.100.20` 和 `203.0.113.30` 是文档示例地址，生成正式工件前必须替换。
- 远程发布地址默认留空，避免玩家入口误连本机或示例服务。
- 示例使用宽屏侧栏模板、两个区服、主备微端和一篇内置公告。
