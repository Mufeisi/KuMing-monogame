# DB-05 任务声明

- 基线：`1828fbb`
- 目标：生产 `SaveDelay` 强制 1～5 分钟，并以确定性故障注入验证最坏调度窗口。
- 做：共享 RPO 策略、Settings 载入、ConfigForm 保存、正式启动与 Envir 调度接线、T-10 证据。
- 不做：改写保存线程或事务、DB-04 恢复、DB-06 MySQL 门槛。
- 方法约束：复用既有 `ProductionSecurityPolicy` 和主循环；不触碰玩家线程状态；存储提交失败不伪报为可保证 RPO。
- 完成定义：1/5 通过，负数/0/>5 正式失败关闭；最坏崩溃注入窗口小于 5 分钟；专项、全量和构建通过。
- 语言：交流、文档、提交信息全部中文。
