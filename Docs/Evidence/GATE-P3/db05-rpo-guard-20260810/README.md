# DB-05 验证证据

## 结论

正式环境 `SaveDelay` 仅允许 1～5 分钟，载入、界面保存、正式启动三处均复用同一策略；Envir 首次和后续自动保存截止时间也复用同一计算。最坏 5 分钟配置下，在下一次保存前 1ms 注入崩溃，未持久化窗口 299999ms，小于 5 分钟。DB-05 不宣称存储提交失败期间仍能保证 RPO。

## 命令与结果

1. DB-05 与正式安全专项：16/16 通过，0 失败，0 跳过；证据 `db05-targeted.trx`。
2. Base05 全量：291/291 通过，0 失败，0 跳过；证据 `db05-base05-full.trx`。
3. Server.Library Release 构建：0 错误，2 条仓库既有包漏洞警告。
4. Server.MirForms Release 构建：0 错误，451 条仓库既有包漏洞/可空性/线程分析警告。
5. `git diff --check`：通过。

## 阶段边界

DB-04 恢复演练仍需单独完成审查修复；DB-06 MySQL 切换门槛尚未执行。DB-05 单独完成不关闭 GATE-P3。
