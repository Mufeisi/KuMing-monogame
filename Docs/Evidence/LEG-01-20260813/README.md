# LEG-01 StartGame、进图、移动与退出证据

- 证据日期：2026-08-13
- 起始提交：`249f79e37f875c6910999edf6af750719951819f`
- 任务分支：`codex/leg-01-real-launch`
- 活动规格：[`../../requirements/LEG-01-真实启动与进图验证.md`](../../requirements/LEG-01-真实启动与进图验证.md)
- 语言：中文，命令、代码标识符与原始类型名除外

## 结论

真实 PC Release 客户端已在窗口模式下完成登录、选角、`StartGame`、地图与玩家信息接收、一次移动、正常退出，并以同一账号再次启动复验。两次进图均跨过外部服务端发出的当前客户端未知完整帧 `Id=275`，后续 `MapInformation` 与 `UserInformation` 未再被同一接收缓冲误丢弃。最终两次客户端退出码均为 `0`，对应时段没有新增异常；服务端记录角色连接与正常断开。

截图 [`窗口模式进图并移动.png`](窗口模式进图并移动.png) 显示比奇省内角色及移动后坐标 `(288,613)`。截图不含密码、聊天隐私或外部个人信息。

## 根因与最小修复

1. `Packet.ReceivePacket` 遇到未知包时会返回 `null` 并清空剩余缓冲；外部服务端的 `Id=275` 与地图、玩家信息位于同一 TCP 缓冲，导致客户端连同后续已知帧一并丢弃。
2. PC 网络接收循环现在只跳过一个长度完整的未知帧，并继续解析同缓冲中的后续帧；协议编号与 `Shared` 事实源均未修改。
3. 进图首帧在 `UserInformation` 到达前不再读取空玩家对象；经验、负重及游戏帧绘制都有空状态保护。
4. 退出时先停止空闲更新/渲染循环，再释放 `DXManager` 与声音资源，消除关闭窗口后同帧继续创建地面纹理的竞态。
5. 冒烟覆盖只在 `LYOCRYSTAL_LEG01_SMOKE=1` 时启用；密码仅从进程环境读取，账号、角色、密码和冒烟开关不写回配置。

## 真实窗口门禁

| 检查 | 结果 |
|---|---|
| 窗口模式 | `Title=Legend of Mir 2`，窗口句柄非零，`Responding=True` |
| 登录与选角 | 临时账号注册成功；建角成功；选择角色成功 |
| StartGame 后收包 | `StartGame`、`MapInformation`、`UserInformation` 均收到 |
| 未知帧兼容 | 跳过单帧 `Id=275`，后续已知帧继续解析 |
| 进图 | 地图文件 `0` 加载，玩家从 `(288,616)` 初始化 |
| 移动 | 客户端发送 `Walk`；服务端返回 `UserLocation`；移动到 `(288,615)` |
| 退出 | 进程退出码 `0`；服务端记录角色断开；本轮无新增异常 |
| 再次启动 | 同一账号再次登录、选角、进图并从 `(288,614)` 移动到 `(288,613)`，退出码 `0` |
| 秘密边界 | `Mir2Config.ini` 未命中本轮账号、角色或运行密码 |

关键收发摘要：

```text
11:40:47 RECV StartGame
11:40:47 RECV MapInformation
11:40:47 RECV UserInformation
11:40:48 SEND Walk
11:40:48 RECV UserLocation
11:43:48 再次启动后收到 MapInformation 与 UserInformation
11:43:50 再次移动成功 From=288,615 To=288,614
```

## 自动探针与局部回归

- 外部协议探针：通过；输出 `status=passed`，阶段包含 `process`、`login`、`character`、`enter-game`、`shutdown`，地图为 `1`，移动已确认。
- PC 接收/首帧/冒烟专项：3/3 通过。
- 启动器常规专项：85/85 通过；覆盖完整客户端包、本地登录核心命中、微端就绪与不可达接缝。
- GATE-L0 强停专项未重跑成功：当前工作区缺少其要求的两个历史真实玩家 EXE；测试按设计失败关闭，没有退化为样品。本项不否定本次窗口直启门禁，但仍阻止把整个 LEG-01 声明为完成。
- PC Release 构建：0 错误；保留一个既有 `WindowsBase` 版本冲突警告。
- `git diff --check`：通过，仅有工作树换行提示。

## 运行制品指纹

| 制品 | 产品版本 | 字节 | SHA256 |
|---|---|---:|---|
| `Build/Client_VorticeDX11/Client.exe` | `1.0.0+249f79e37f875c6910999edf6af750719951819f` | 179712 | `5F86E0D02DAD67A28CE16B2A6575B9069A6C9851742C923230A0F5649CFA8DE9` |
| `D:/ChuanQi/Crystal_monogame/Server-mono/Server.exe` | `1.0.0+9af0616f6d6e31169e2614a546179d47e516b881` | 233977581 | `53A6BE3484966E9C43DFF4F5C5A20DD37FCD2A76386202D32D496988AFF4DAEB` |
| `artifacts/启动器配置器-本地完整客户端优先-20260812/传奇启动器配置器-本地完整客户端优先版.exe` | `1.0.0+cdc06435f0f8dd05752c397d87f49e1a8cae5e3c` | 223662202 | `E8C7EC6E7D2B00A5697AB10ECF136CDB0BE13F1C81F4379AA448A37A1FC6C9C8` |

验证时服务端实际监听 `127.0.0.1:7000`，微端 HTTP 实际监听 `127.0.0.1:7777`。本次测试只新增专用账号与角色，不删除或覆盖外部运行数据，不执行正式发布。

## 每日工件检查

- 工件：客户端接收链路修复、进图空状态保护、窗口冒烟控制、退出竞态修复、协议探针、专项测试、真实进图截图和运行证据。
- 过程资产：本证据 README 一份；数量少于可运行工件。
- 语言：文档、状态和提交信息使用中文。

## 回滚

代码回滚到起始提交即可关闭本次兼容与冒烟改动。外部测试数据按活动规格保留，不通过删除数据库回滚；构建目录与截图不是发布来源。
