# RELEASE-01 任务声明

- 目标：完成 APK 与资源清单两套独立签名密钥、受保护短暂取用、生产信任表、轮换和 T-07 发布安全验证。
- 做：生产资源公钥接线；受保护资源私钥与 APK keystore 管理；确定性资源索引签名；CI 密钥注入/清理；正确签名、篡改、防降级和密钥混用测试。
- 不做：不提交任何私钥、口令或明文密钥；不实现 RELEASE-02 灰度/回滚流水线；不执行 RELEASE-03 真机验收。
- 方法约束：复用 SEC-06 `BootstrapManifestSignaturePolicy`、Android 平台签名参数和 CI secret；两类密钥用途隔离并失败关闭。
- 预估时间：约 2～4 小时；慢构建只在阶段末执行。
- 完成定义：无密钥时发布步骤失败；资源签名可被正式信任表验证且覆盖全部包；APK 使用独立 keystore 签名并可验证；T-07、Base05、Release 构建与双轴复审通过。
- 语言：交流、文档、证据和提交信息全部使用中文；代码标识符和命令保留英文。
- 分支：`codex/p5-release01-20260810`
- 基线：`4b66392`
- 状态：进行中。
