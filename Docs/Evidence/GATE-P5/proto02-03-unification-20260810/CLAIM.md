# PROTO-02/03 任务声明

- 目标：让 PC、移动端与服务端的正式构建直接消费同一份 `Shared` C# 协议源，并以自动生成清单和 CI 校验阻止 wire 漂移；同时固化客户端、服务端、协议与资源最低兼容矩阵。
- 做：分阶段切换移动端正式构建、生成并校验协议清单、补自动测试与 CI、编写兼容矩阵、归档运行证据。
- 不做：不修改任何协议字段、包 ID 或序列化语义；不删除 `Share` 历史副本；不进入 RELEASE-01～03。
- 方法约束：复用 `Shared`、现有 `ProtocolGoldenTests`、SEC-06 资源版本门禁和现有 CI；历史副本仅保留为兼容测试夹具。
- 预估时间：约 2～3 小时；单次验证超过 30 分钟时只在阶段末执行慢外环。
- 完成定义：移动端 Release 构建、协议专项测试、Base05 全量测试和 PC/Server 构建通过；生成清单无漂移；兼容矩阵与证据可复核；双轴复审无阻断。
- 语言：交流、文档、证据和提交信息全部使用中文；代码标识符和命令保留英文。
- 分支：`codex/p5-proto02-03-20260810`
- 基线：`3bd9403`
- 状态：进行中。
