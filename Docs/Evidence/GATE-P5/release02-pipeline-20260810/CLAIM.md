# RELEASE-02 任务声明

- 目标：交付构建、冒烟、签名、导出、灰度和回滚的一键发布闭环，并让资源更新按验证后切换、失败恢复上一可运行版本。
- 做：复用现有 Bootstrap、RELEASE-01 签名、OPS 监控/崩溃/Kill Switch 接缝；实现发布编排、事务状态、灰度判定、失败回滚和专项证据。
- 不做：不执行真实渠道上线；不做 RELEASE-03 真机生命周期、APK 密钥灾难恢复；不改协议格式、渲染、脚本或数据库 Schema。
- 方法约束：外部 Secret 缺失失败关闭；不记录口令；不重造 Bootstrap；玩家状态不跨主线程修改；慢发布验证只在阶段末执行。
- 完成定义：一键命令可生成可验签发布工件；故障注入证明失败恢复上一版本；灰度错误率/崩溃率达到阈值自动回滚；专项、全量、Release 构建与双轴复审通过。
- 语言：交流、文档、状态和提交信息全部中文。
