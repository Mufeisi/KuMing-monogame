# 桌面客户端重新编译发布记录

语言：中文

- 基线：当前任务分支，基于 `54b3dba`。
- 编译：`dotnet build Client_VorticeDX11/Client_VorticeDX11.csproj -c Release --nologo -v:minimal`。
- 结果：0 个错误，38 个既有警告。
- 发布：已将本次 Release 客户端二进制、依赖、启动资源清单和运行配置发布到指定的 `Client_VorticeDX11` 目录；保留该目录已有 `Data`、`Map`、`Sound`、`Cache` 等资源目录。
- 启动复核：目标目录中的 `Client.exe` 已启动，窗口标题为“启动器”，进程响应正常。
- `Client.exe` SHA-256：`6A66ACD21AB9E9957F1D17291FF54BC375869B9D6A5F86052079AC8749BFCB65`。
- 证据边界：本文件不记录账号、密码、用户名、主机名或用户目录。
