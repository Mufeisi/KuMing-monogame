# PERF-03 任务声明

- 基线：`d68d3d2`
- 目标：从 `HumanObject` 提取有真实接口的装备套装加成深模块，并以回归证明数值行为不变。
- 做：普通套装、圣龙戒指、神龙部位与天龙组合规则迁移；接口级测试；PRD 压力测试口径按用户决定更新。
- 不做：无基线热点优化、ArrayPool/预分配、渲染合批、协议/脚本/Schema 改动、自动压力测试。
- 方法约束：纯进程内模块；不新增假适配器；不使用 `partial`；保持玩家状态主线程边界和原执行顺序。
- 完成定义：巨型类移除原规则；模块接口回归、Base05 全量、Server.Library 与 Server.MirForms Release 构建通过。
- 语言：交流、文档、提交信息全部中文。
