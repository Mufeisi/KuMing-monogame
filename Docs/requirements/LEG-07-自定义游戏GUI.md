# LEG-07：自定义游戏 GUI

- 状态：已激活；`GUI-CORE-01..04` 与 `GUI-01..05` 已完成，`GATE-GUI-CORE` 已关闭，下一切片为 `GUI-06` 静态双端显示与签名发布冒烟
- 负责人：项目所有者
- 激活日期：2026-08-14
- 最后复核日期：2026-08-14
- 事实职责：维护 LEG-07 的产品目标、实施顺序、验收门禁、风险边界和回滚入口
- 上位路线：[`传奇类参考源码吸收开发路线.md`](传奇类参考源码吸收开发路线.md)
- 专项设计基线：[`自定义启动器-微端-游戏GUI推进开发路线.md`](自定义启动器-微端-游戏GUI推进开发路线.md)
- 作者工具规范：[`../design/launcher/桌面作者工具GUI设计规范.md`](../design/launcher/桌面作者工具GUI设计规范.md)
- 游戏 GUI 运行描述：[`../design/custom-gui/游戏GUI运行描述规范.md`](../design/custom-gui/游戏GUI运行描述规范.md)
- 前置任务：[`LEG-04-启动器作者体验.md`](LEG-04-启动器作者体验.md)、[`LEG-05-微端与发行体作者体验.md`](LEG-05-微端与发行体作者体验.md)和[`LEG-06-内容生产工作台.md`](LEG-06-内容生产工作台.md)均已完成
- 语言：中文，代码标识符、命令及原始测试字段除外

## 1. 目标与用户价值

版本作者可以在既有专业作者工具中设计安全的游戏内窗口，以同一份经过签名和版本约束的运行描述在 PC 与 Android 显示，并最终以一个活动兑换窗口完成服务端权威业务闭环。作者不需要手写坐标或运行时代码，客户端也不能决定奖励、价格、物品归属或玩家状态。

## 2. 任务简报

- 目标：依次关闭 `GATE-GUI-CORE`、`GATE-GUI-STATIC`、`GATE-GUI-DYNAMIC`，交付一个可设计、可验证、可发布、可回滚的双端自定义游戏窗口闭环。
- 用户价值：版本作者复用成熟画布工作流生产游戏窗口；玩家在 PC 与 Android 获得一致体验；业务结果由服务端权威验证。
- 做：设计核心深化、游戏 GUI Schema、作者 Adapter、PC/移动运行 Adapter、签名与资源限制、服务端窗口会话、白名单动作、状态投影及活动兑换纵向闭环。
- 不做：通用脚本 UI、任意反射/文件/URL 调用、客户端权威奖励、重写 FairyGUI、重写 `DXManager`/`SpriteBatchStack`、重写协议序列化、商城/交易/强化等第二业务场景。
- 方法约束：每个切片独立分支、提交、测试和证据；上一门禁未关闭不得进入下一门禁；优先复用现有签名资源、微端、渲染与协议接缝。
- 风险等级：高。涉及跨端运行描述、签名输入、资源上限、协议边界和玩家状态权威性。
- 预估成本：12～20 人周；这是阶段拆分依据，不是发布日期承诺。
- 完成定义：同一签名 GUI 包在 PC 与 Android 正确显示并由服务端打开、投影、处理白名单动作和关闭；篡改、过期、未知类型、越界资源与重放均失败关闭；一个活动兑换窗口完成真实双端验收。
- 验证方式：无 UI 领域测试、作者工具 Windows 集成、PC/Android 运行适配测试、协议兼容与攻击样例、签名/降级/资源上限测试、真实双端截图或录屏及测试服业务冒烟。
- 回滚方式：按独立切片回滚代码；客户端 GUI 包按已接受序列和签名版本回退；动态窗口由服务端 Kill Switch 停用并关闭现有会话；Schema/协议一旦进入兼容窗口按专项迁移说明处理。
- 语言：全部交流、规格、状态、证据与提交信息使用中文。

## 3. 当前基线与真实接缝

1. 通用设计行为的当前事实实现为 `src/Launcher/Launcher.DesignCore/CanvasDocument.cs` 及 `ICanvasDocument<TId>`；选择、几何变换、吸附、对齐、层级、锁定、隐藏、撤销/重做和脏状态已由启动器真实 UI 通过公共接口消费。
2. `src/Launcher/Launcher.Editor/LauncherCanvasDocument.cs` 是启动器主题文档到设计核心的 Adapter；WinForms 对象树、画布和属性检查器不拥有第二套选择、历史或几何规则。
3. PC 渲染必须走现有 `DXManager`/MirControls 接缝；移动渲染必须复用内嵌 FairyGUI 与 `SpriteBatchStack`。Adapter 只映射运行描述，不复制渲染器。
4. GUI 包签名、摘要、防降级、主备源和原子切换复用现有 Bootstrap 清单与接受策略；不建立第二套信任根或下载器。
5. 动态协议只允许在静态门禁关闭后，于 `src/Shared/Shared/Packet.cs` 的现有序列化体系中增加真实包；服务端玩家状态仍只由主线程修改。

## 4. 实施顺序

### 4.1 `GATE-GUI-CORE`：设计核心深化

1. `GUI-CORE-01` 设计核心基线与接缝审计：固定当前启动器行为契约、依赖边界、公共接口、迁移顺序和回归入口；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-CORE-01.md`](../Evidence/LEG-07-20260814/GUI-CORE-01.md)。
2. `GUI-CORE-02` 深模块提取：将文档、选择、命令、几何、历史、诊断和变更通知移入不引用 WinForms、FairyGUI、MonoGame 或 Vortice 的项目；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-CORE-02.md`](../Evidence/LEG-07-20260814/GUI-CORE-02.md)。
3. `GUI-CORE-03` 启动器 Adapter 接回：对象树、画布、属性检查器仅通过核心接口修改文档，删除旧窗体内部重复逻辑；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-CORE-03.md`](../Evidence/LEG-07-20260814/GUI-CORE-03.md)。
4. `GUI-CORE-04` 内存 Adapter 与门禁：设计行为测试只断言公共可观察结果，现有 `GATE-UXL` 行为和截图无回归；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-CORE-04.md`](../Evidence/LEG-07-20260814/GUI-CORE-04.md)。

`GATE-GUI-CORE` 已关闭。退出条件以专项路线为准：启动器无回归、核心无 UI/渲染依赖、测试只走接口、旧逻辑不与新核心并存。

### 4.2 `GATE-GUI-STATIC`：双端静态窗口

1. `GUI-01` 游戏 GUI 设计文档和运行描述 Schema：首版只包含 `Window/Panel`、`Image`、`Text/RichText`、`Button`、`TextInput`、`List`、`ProgressBar`、`ItemSlot` 及专项路线规定的布局能力；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-01.md`](../Evidence/LEG-07-20260814/GUI-01.md)。
2. `GUI-02` 作者工具游戏 GUI Adapter，复用设计核心和既有三栏工作区，不增加新的永久平级侧栏；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-02.md`](../Evidence/LEG-07-20260814/GUI-02.md)。
3. `GUI-03` PC Adapter，通过现有 `DXManager` 与 MirControls 映射静态控件；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-03.md`](../Evidence/LEG-07-20260814/GUI-03.md)。
4. `GUI-04` 移动 Adapter，通过现有 FairyGUI 与 `SpriteBatchStack` 映射同一运行描述；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-04.md`](../Evidence/LEG-07-20260814/GUI-04.md)。
5. `GUI-05` 资源、字体、图集、安全区、数量、层级、文本与总包大小校验；未知控件、未知属性、循环引用和超限资源失败关闭；已完成，证据见 [`../Evidence/LEG-07-20260814/GUI-05.md`](../Evidence/LEG-07-20260814/GUI-05.md)。
6. `GUI-06` 静态“新手活动窗口”双端显示与签名发布冒烟。

### 4.3 `GATE-GUI-DYNAMIC`：服务端权威活动窗口

1. `GUI-07` Shared 真实协议包与严格上限。
2. `GUI-08` 服务端窗口会话、GUI 版本绑定、随机数和重放保护。
3. `GUI-09` 主线程验证动作、文本、选择、物品所有权、距离、NPC、活动、货币和次数。
4. `GUI-10` PC/Android 状态投影和有界增量更新。
5. `GUI-11` 脚本旁路 Hook，只允许声明窗口、提供数据和处理登记的白名单动作。
6. `GUI-12` 一个活动兑换窗口完成 PC/Android/服务端真实闭环。

## 5. 不变量与失败模式

1. 同一设计文档只能生成确定版本的运行描述；构建与运行不得各自解释一套语义。
2. 客户端只提交动作意图和受限输入，不提交价格、奖励结果、权威物品属性或次数结论。
3. 每个动作绑定玩家、窗口实例、GUI 版本、会话随机数和有效期；未知、过期、乱序或重放请求必须拒绝。
4. 所有列表、文本、资源、控件数、嵌套深度、包体和协议载荷都有显式上限；解析失败不得降级为任意运行。
5. GUI 包只有通过现有信任链、签名、摘要、兼容版本和防降级校验后才能加载；发布失败保留上一已接受版本。
6. PC 和 Android Adapter 不拥有业务状态，不绕过现有渲染接缝，不在后台线程修改玩家状态。
7. 设计核心只拥有编辑行为，不拥有启动器主题、游戏 GUI Schema 或运行时控件类型；这些由各自 Adapter 映射。
8. 保存、发布和版本切换使用临时输出、完整校验和原子替换；失败必须可重试且不破坏原事实。

## 6. 验证与证据

### 6.1 快内环

- 设计核心领域测试及依赖边界检查。
- `Launcher.PlayerShellIntegration.Windows` 作者行为回归。
- Shared Schema/签名/上限测试。
- PC 与移动 Adapter 的相同运行描述投影测试。
- 受影响项目 Release 构建及 `git diff --check`。

### 6.2 阶段外环

- `GATE-GUI-CORE`：启动器真实画布与四档 DPI 无回归。
- `GATE-GUI-STATIC`：同一签名 GUI 包在 PC 与 Android 显示标题、图片、列表、进度和按钮；保存双端截图与诊断样例。
- `GATE-GUI-DYNAMIC`：测试服服务端打开活动兑换窗口，双端完成一次合法兑换；篡改、重放、过期、断线重连和版本切换全部有明确结果及日志证据。

证据按切片写入 `Docs/Evidence/LEG-07-YYYYMMDD/`；Evidence 只记录执行事实，不承担动态状态。

## 7. 阶段退出条件

1. `GATE-GUI-CORE`、`GATE-GUI-STATIC`、`GATE-GUI-DYNAMIC` 依次关闭，且无越级实现或长期双轨核心。
2. 版本作者可在作者工具中设计静态活动窗口，无需手写 JSON 或坐标文本。
3. 同一签名运行描述在 PC 与 Android 正确显示，未知或超限输入失败关闭。
4. 一个真实活动兑换窗口由服务端权威完成打开、数据投影、合法动作、拒绝非法动作、更新和关闭。
5. 客户端不能授予奖励、决定价格、伪造物品或绕过次数/距离/活动校验；玩家状态只在服务端主线程修改。
6. 构建、领域测试、Windows 集成、双端运行、安全失败样例、测试服冒烟、截图/录屏和回滚证据全部通过。

## 8. 已完成切片 `GUI-01`

### 做

1. 在 `Shared.CustomGui` 定义 Schema v1、跨端视口、锚点、边距、横纵流布局及九个具体运行类型。
2. 通过 `type` 判别和稳定 `id/parentId` 表达扁平对象图；PC 与 Android 共用同一文档模型和源生成 JSON 元数据。
3. 生产 Codec 使用 UTF-8、camelCase、字符串枚举和严格未知字段策略；未知控件、未知属性、未知枚举、缺失必填字段与不兼容版本全部失败关闭。
4. 交付中文线格式规范和可由生产 Codec 直接读取的“新手活动”示例运行描述。

### 不做

1. 不实现作者工具画布和属性编辑；这些属于 `GUI-02`。
2. 不实现 PC/Android 游戏 GUI Adapter；这些属于 `GUI-03/04`。
3. 不实现资源上限、父图循环与安全区完整校验；这些属于 `GUI-05`。
4. 不修改 Shared 协议、服务端窗口会话或玩家状态。

### 完成定义

Schema v1 可确定性往返全部首版控件与跨端布局；生产 Codec 严格拒绝未知或不兼容输入；仓库示例可直接读取；Base05 全量、启动器 Windows 全量和 Android 目标构建通过，并产生独立提交与证据。

## 9. 已完成切片 `GUI-02`

### 做

1. `CustomGuiCanvasDocument` 将 Schema v1 运行描述适配到既有 `ICanvasDocument<string>`，选择、移动、锁定、显隐、层级、撤销/重做和脏状态继续由设计核心统一承载。
2. 运行事实只保存跨端 Schema；锁定等作者元数据按 `documentId + elementId` 独立保存在编辑器项目中，不污染运行描述。
3. 既有“设计”模式内部提供“启动器界面/游戏界面”文档切换，游戏界面继续使用对象树、中央画布、属性栏三栏结构，不增加新的顶级模式或永久侧栏。
4. 新项目提供覆盖九个首版控件类型的可编辑活动窗口；对象树选择、属性修改、键盘微移、显隐、锁定、对齐及保存重载均走公共设计核心接口。

### 不做

1. 不实现 PC 或 Android 运行时控件映射；分别属于 `GUI-03/04`。
2. 不实现资源、父子循环、数量和总包上限校验；属于 `GUI-05`。
3. 不修改 Shared 协议、服务端窗口会话、玩家状态或脚本 Hook。

### 完成定义

游戏 GUI 文档在现有专业作者工具三栏工作区中可见、可选、可编辑、可撤销并原子随项目保存重载；作者元数据与运行描述分离；启动器设计工作区及顶级五模式无回归；Windows 全量、Release 构建和后台 1280×800/1100×700 截图通过。

## 10. 已完成切片 `GUI-03`

### 做

1. Shared 提供唯一的父级、锚点、拉伸、边距、间距和横纵流布局解析入口；作者 Adapter 与 PC Adapter 消费同一结果，不各自解释一套坐标语义。
2. PC Adapter 将九个 Schema v1 控件类型物化为现有 `MirControl/MirLabel` 控件树；窗口、面板、图片、文本、按钮、输入占位、列表、进度和物品格均保留运行描述标识与静态内容。
3. `fit` 视口按等比缩放和居中留边转换为目标像素；字体按目标比例直接生成，控件不依赖拉伸后的离屏截图。
4. 图片和物品格只通过逻辑 `assetId` 解析为既有 `MLibrary`，实际绘制继续调用 `MLibrary/MirControl` 的 `DXManager` 接缝；Adapter 不拥有第二渲染器。
5. Host 可显式附着到现有场景控件树并统一释放；父级循环在创建任何 MirControl 前以 `GUI03-LAYOUT-001` 失败关闭。

### 不做

1. 不实现 Android/FairyGUI 物化；属于 `GUI-04`。
2. 不完成资源存在性、字体白名单、数量、深度和包体上限；属于 `GUI-05`。
3. 不修改协议、服务端会话、脚本 Hook 或玩家状态；真实 PC 截图与双端对照在 `GUI-06` 阶段外环产生。

### 完成定义

同一 Schema v1 文档经共享布局引擎确定性解析，并在 PC 端形成可附着、可释放的 MirControls 控件树；九个首版类型、等比视口、资源接缝、父子坐标、静态列表、进度和失败关闭均有公共可观察测试，Base05 与 Windows Release 全量通过。

## 11. 已完成切片 `GUI-04`

### 做

1. 移动 Adapter 与 PC、作者工具共同消费 `Shared.CustomGui` Schema 和 `CustomGuiLayoutEngine`，统一父级、锚点、拉伸、边距和流布局语义。
2. 可测试 Adapter 负责 `fit` 缩放、竖屏居中留边、父子树、层级、显隐及九类节点语义；Android 工厂仅将节点物化为仓库内嵌 FairyGUI 对象。
3. Android 工厂使用 `GComponent/GLoader/GTextField/GRichTextField/GButton/GTextInput/GList/GProgressBar` 等既有类型；图片只接受 `assetId -> UIPackage URL` 解析接缝。
4. Host 可附着到既有 FairyGUI 容器并由根节点统一释放；实际绘制继续进入 `FairyGuiHost -> Stage/FairyBatch`，并处于现有 `SpriteBatchStack` 主场景绘制之后的既定渲染序列。
5. 运行描述在创建任何移动节点前先完成共享布局校验；工厂中途失败会释放已物化树，不遗留半成品窗口。

### 不做

1. 不完成资源存在性、字体白名单、安全区、数量、深度、文本和包体上限；属于 `GUI-05`。
2. 不挂载静态示例包或产生真实 Android 截图；属于 `GUI-06` 与 `GATE-GUI-STATIC` 阶段外环。
3. 不修改协议、服务端会话、脚本 Hook、玩家状态或 FairyGUI/SpriteBatchStack 运行时。

### 完成定义

同一 Schema v1 文档可确定性投影为移动控件树；九个首版类型、竖屏等比视口、父子坐标、静态内容、工厂失败清理和循环父级失败关闭均有公共行为证据；Android Release 目标、Base05 与 Windows Release 全量通过。

## 12. 已完成切片 `GUI-05`

### 做

1. 在 Schema Codec 之后增加唯一语义校验入口，失败关闭非法对象图、循环或过深父级、越过父级/安全区的布局、非法富文本、未知资源及各项数量上限。
2. 逻辑资源、字体和图集只绑定到既有 Bootstrap 资源清单中的物理资产；不建立第二份下载清单、资源事实源或信任根。
3. GUI ZIP 使用固定运行描述与资源绑定入口，并复用既有 Bootstrap 签名策略、包摘要策略和发布序列防降级；校验器直接读取被签名 ZIP，拒绝调用方替换已签名内容。
4. 对包体、ZIP 条目、解压总量、运行描述、资源绑定、控件、嵌套、列表、文本和输入长度设置显式上限；路径穿越、重复条目、同修订内容替换及降级均失败关闭。

### 不做

1. 不生成或发布静态示例签名包，不挂载 PC/Android 真实场景；属于 `GUI-06`。
2. 不增加协议、服务端会话、脚本 Hook、玩家状态或动态绑定行为；属于 `GUI-07..12`。
3. 不重写 Bootstrap 清单、签名、摘要、微端下载器、DX/FairyGUI 渲染器或协议序列化。

### 完成定义

合法运行描述和 Bootstrap 资源绑定可通过同一校验入口；所有稳定诊断码、资源缺失、对象图错误、上限、ZIP 攻击、签名篡改和降级路径均有领域测试；PC、Android、作者工具构建与既有全量回归通过，并产生独立提交和证据。

## 13. 回滚

1. 每个 `GUI-*` 切片独立提交，可按逆序回滚；不得用一次提交跨越两个阶段门禁。
2. `GUI-CORE-*` 回滚必须恢复启动器 Adapter 与核心的匹配版本，禁止留下两套编辑逻辑。
3. 静态 GUI 包通过现有资源版本切换回上一已接受签名版本；客户端遇到不兼容版本失败关闭而非解释旧数据。
4. 动态阶段必须提供服务端 Kill Switch：停止新开窗口并关闭/过期现有会话；代码回滚不得令玩家卡在未完成事务中。
5. 若未来需要 SchemaMigration 或协议兼容窗口，必须在对应切片独立补充迁移与回滚说明，本激活规格不预先授权数据迁移。
