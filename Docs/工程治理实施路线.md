# LyoCrystal 工程治理实施路线

- 状态：进行中
- 负责人：项目所有者
- 最后复核日期：2026-08-12
- 当前阶段：ENG-08（随功能持续）
- 前置规范：[`工程开发规范.md`](工程开发规范.md)
- 事实源：本文只维护工程治理任务状态；产品功能状态由对应活动 PRD 维护

## 1. 目标

- 建立磁盘、解决方案、过滤器和 CI 一致的可信工程地图。
- 统一构建、测试、调试和文档入口，降低新人及 Agent 的理解成本。
- 把可机械检查的 `MUST` 规则逐步落到 CI 和代码托管门禁。
- 为客户端、服务端、启动器、微端、共享、测试和工具明确职责与所有权。
- 随产品任务渐进提取深模块，不阻塞正常功能开发。

## 2. 非目标

- 不一次性移动全部物理目录或替换命名空间。
- 不一次性拆分 `GameScene`、`PlayerObject` 等巨型类。
- 不重写协议序列化、渲染、脚本热更、微端、FairyGUI 或数据库迁移设施。
- 不制造独立治理平台、分析器或第二套任务系统。
- 不用工程整理覆盖既有产品缺陷、功能需求或历史证据。

## 3. 当前问题基线

| 编号 | 问题 | 2026-08-12 基线 | 关闭条件 |
|---|---|---|---|
| B1 | 旧发布 PRD 混合历史与活动状态 | 已关闭：旧 PRD 标记为完成并转入维护，§4.4 明确为历史快照 | 明确 PRD 生命周期，工程治理使用本文作为活动状态源 |
| B2 | 主解决方案未覆盖全部有效工程 | 已关闭：磁盘与主解决方案均为 30 个项目 | 30 个项目全部分类并进入主解决方案 |
| B3 | 子系统开发入口不足 | 已关闭首批缺口：保留 Windows 发布过滤器，新增 Server 与 Launcher 过滤器 | 建立用途明确的 Server、Launcher 等过滤器 |
| B4 | 工程一致性依赖人工发现 | 已关闭：Windows CI 在 restore 后比较磁盘与主解决方案工程集合 | CI 自动检查集合差异 |
| B5 | 构建与依赖规则分散 | 已关闭首批缺口：新增安全默认值与确定性构建；7 个测试工程统一三组工具链版本；移动端继续使用专项属性文件 | 新工程继承默认值，重复测试版本不再漂移；模块有意差异保持显式 |
| B6 | 模块职责主要靠经验理解 | 已关闭：模块地图覆盖 30 个工程的职责、依赖方向、入口、验证和高风险边界；CODEOWNERS 覆盖所有路径 | 主要子系统无需靠目录猜测职责 |
| B7 | 核心存在巨型类 | 多个 7K～16K 行核心类 | 随功能逐个提取有回归保护的深模块 |
| B8 | 协议生成清单存在漂移 | 已关闭：原始 IL metadata token 改为稳定语义身份；两次干净 Release 生成一致且正式清单通过 `--verify` | ENG-04 查明差异来源，修复后由同一命令通过 |

## 4. 治理顺序

| 任务 | 状态 | 主要工件 | 退出条件 |
|---|---|---|---|
| ENG-01 状态与活动计划收口 | 已完成 | 旧 PRD 生命周期标记、本文、文档索引 | 工程治理只有本文维护动态状态，历史任务不再被解释为占用 |
| ENG-02 工程分类与解决方案收口 | 已完成 | 主 `.sln`、Server/Launcher `.slnf`、CI 一致性检查 | 磁盘/主解决方案 30/30 一致；三个过滤器 restore；Server/Launcher Release 构建 0 错误 |
| ENG-03 构建、测试和调试入口统一 | 已完成 | README 快速入口、`CONTRIBUTING.md`、开发者指南 | 环境、解决方案、构建、测试、调试和制品边界均有可执行入口 |
| ENG-04 基础质量门禁 | 已完成 | 文档链接、生成物漂移、最小构建测试门禁 | 150 个已跟踪 Markdown 文件的相对链接可自动检查；协议清单可跨干净构建稳定验证；既有 Windows/Android/通用测试门禁保持生效 |
| ENG-05A 仓库内代码托管治理 | 已完成 | PR 模板、CODEOWNERS、分支保护方案 | 变更模板覆盖规格与证据；高风险模块有有效所有者；目标检查与现有 CI job 一致 |
| ENG-05B 远端规则应用 | 待发布后执行 | GitHub `main` ruleset、验证 PR | 治理提交到达远端并完成首轮 CI 后，必需检查作用于最新提交且保护规则实测有效 |
| ENG-06 构建配置与依赖收口 | 已完成 | `Directory.Build.props`、测试工具链版本属性、开发者说明 | 30 个现有工程保持显式行为；新工程继承安全默认值；7 个测试工程统一版本且过滤器构建通过 |
| ENG-07 模块地图与所有权 | 已完成 | 模块职责、接口、依赖、运行入口、测试入口与 CODEOWNERS | 主要子系统可由统一地图定位，依赖和高风险边界明确 |
| ENG-08 渐进深模块化 | 持续 | 回归测试、深模块及小接口 | 只在真实功能触达时逐个关闭巨型行为簇 |
| ENG-09 物理目录重排评估 | 最后评估 | 是否迁移 `src/tests/tools` 的决定 | 只有收益超过路径与历史成本时实施 |

## 5. ENG-02 工程分类

主解决方案使用逻辑目录表达架构，不改变物理路径：

| 分类 | 工程 |
|---|---|
| Clients | `Client_VorticeDX11`、`Client_MonoGame.Shared`、Android、iOS |
| Server | `Server.Library`、`Server.MirForms` |
| Shared | `Shared` |
| Launcher | `Launcher.PlayerShell.Core`、`Launcher.PlayerShell`、`Launcher.ThemeRuntime`、`Launcher.Editor` |
| MicroGateway | `MicroGateway.Core`、`MicroGateway.App` |
| Editors | `AutoPatcherAdmin`、`Controls`、`LibraryEditor`、`LibraryViewer` |
| Tests | `Base05.Tests`、`ShareProtocolCompat` |
| Tools | 四个正式工具工程 |
| WindowsIntegration | 七个 Windows 集成/门禁工程 |

`LyoCrystal.Windows.slnf` 继续表示现有 Windows 发布基线，不因主解决方案补齐而扩大 CI 构建面。新增过滤器只用于按子系统开发和验证。

## 6. 执行规则

1. 当前只执行状态为“进行中”的任务；不得提前进行物理目录移动。
2. 每项任务使用独立分支和提交，变更后按《工程开发规范》验证。
3. 状态变更必须有代码、配置、测试输出或可复核对比数据支持。
4. 发现产品缺陷只进入对应产品 backlog，不混入工程治理提交。
5. 每项治理任务完成后更新本文状态，并明确下一项。
